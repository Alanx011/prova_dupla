
function contarAte(inicio, fim) {

	//if(isNaN(inicio) || isNan(fim)) throw new Error ('Isso não é um numero!');

let arr = []

let cont = inicio;

while (cont <= fim) {

console.log(cont);
cont ++ ;
		}
 }

console.log(contarAte(1, 8))


